export enum difficulty {
  UNKNOWN,
  EASY,
  MEDIUM,
  HARD,
}

export enum stat {
  STAMINA = 1,
  STRENGTH,
  CRITICAL_RATING,
  DODGE_RATING,
}

export enum itemQuality {
  UNKNOWN,
  COMMON,
  RARE,
  EPIC,
}

export enum resource {
  UNKNOWN,
  QUEST_REDUCTION,
  SLOTMACHINE,
  SLOTMACHINE_JETON,
}
